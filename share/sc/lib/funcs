import_funcs() {
    local _dir _f

    _dir="$1"
    shift
    
    for _f in ${_dir}/*; do

	case "$_f" in
	    *.sh)
		. ${_f}
		;;
	    *)
		true
		;;
	esac
    done
}

import_funcs $TOP/share/sc/lib
